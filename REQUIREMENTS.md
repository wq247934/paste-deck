# PasteDeck 待评估功能项

> 已实现或明确不需要的旧条目已清理。

## 1. 图片 OCR 搜索

- 当前图片搜索已支持按图片尺寸、来源应用、收藏夹名、自定义别名等元数据检索。
- 未实现图片内容 OCR 识别。
- 建议方案：如果要做 OCR，使用 Vision 框架后台异步识别，并把结果缓存到模型字段中；不要在剪贴板监听、面板打开、实时搜索路径里同步执行 OCR。
- 性能判断：实现得当时不会明显影响日常运行速度；如果在复制图片时同步识别，可能拖慢剪贴板记录并增加 CPU/内存占用。

## 2. 正式版测试数据注入

- 当前应用在数据库为空时会注入测试数据，适合开发验证，不适合正式版本。
- 建议：移除启动注入逻辑，或限定在 `DEBUG` 构建中启用。
