# PasteDeck 功能需求文档

> 已实现功能与待评估功能项。

---

## 已实现功能

### 1. 剪贴板记录与类型识别

- [x] 记录文本、富文本、链接、Markdown、JSON、图片、文件、颜色
- [x] 按优先级检测内容类型：fileURL → image(tiff/png) → string(link/json/markdown/text) → color
- [x] 保留 RTF 数据用于富文本粘贴
- [x] 图片缓存到 `~/Library/Caches/PasteDeck/images/`，缓存大小可配置（100MB/500MB/1GB/无限）
- [x] 去重处理
- [x] 应用黑名单过滤（基于 Bundle ID）

### 2. 全局快捷键与面板

- [x] 全局快捷键 `Command + Shift + V` 打开/关闭面板
- [x] 支持自定义快捷键录制
- [x] NSPanel 浮动窗口，居中显示，毛玻璃效果
- [x] 菜单栏图标与下拉菜单

### 3. 搜索与导航

- [x] 实时模糊搜索
- [x] 键盘导航：左右移动时自动将边缘选中卡片滚入当前可视区域、上下按当前面板宽度翻页、Tab 切换筛选
- [x] 多选扩展（Shift + 方向键）
- [x] 链接按网站名搜索

### 4. 卡片交互

- [x] 单击选中、双击粘贴
- [x] Enter 粘贴、Shift+Enter 纯文本粘贴
- [x] Space 预览
- [x] 右键菜单：复制/删除/置顶/收藏
- [x] 收藏夹与自定义集合
- [x] 批量粘贴

### 5. 预览窗口

- [x] 文本预览（可编辑）
- [x] 代码语法高亮（可编辑）
- [x] Markdown 渲染预览
- [x] JSON 格式化预览
- [x] 图片预览（原始尺寸）
- [x] 图片文件预览
- [x] 文件元信息预览
- [x] 链接预览
- [x] 颜色预览（色块 + hex，可编辑）
- [x] 百度翻译集成

### 6. 面板布局

- [x] 横向卡片布局（默认）
- [x] 竖向面板布局
- [x] 竖向紧凑列表样式
- [x] 竖向大卡片样式
- [x] 竖向自适应网格样式

### 7. 设置

- [x] 通用：开机启动、权限管理
- [x] 快捷键：自定义录制
- [x] 历史记录：条数/时间限制、缓存限制、自动清理
- [x] 统计：使用洞察与趋势
- [x] 过滤：应用黑名单
- [x] 收藏夹：管理名称和顺序
- [x] 外观：主题切换（亮色/暗色/跟随系统）、面板布局方向与样式
- [x] 高级：预览配置、翻译设置、数据维护

### 8. 统计功能

- [x] 菜单栏统计概览（今日复制/总条数/缓存占用）
- [x] 每日趋势图（7 天/30 天）
- [x] 类型分布
- [x] 来源 App 洞察
- [x] 极值记录
- [x] 每日聚合快照（DailyStatsSnapshot），避免全表扫描

### 9. 链接增强

- [x] 链接卡片显示网站名称
- [x] 按网站名搜索链接历史
- [x] 自动抓取复制链接的网页标题（LinkTitleService）

### 10. 外观管理

- [x] 亮色主题
- [x] 暗色主题
- [x] 跟随系统
- [x] 主面板、预览窗口、设置页统一生效（AppearanceManager）

### 11. OCR

- [x] 图片复制后自动 OCR
- [x] OCR 识别文本参与搜索

### 12. 打包发布

- [x] DMG 安装包构建脚本
- [x] 本地代码签名
- [x] GitHub Release 自动发布
- [x] SemVer 版本管理

### 13. 测试

- [x] 面板布局几何测试（MainPanelLayoutTests）
- [x] 面板帧几何测试（MainPanelFrameGeometryTests）
- [x] 设置布局测试（AppSettingsLayoutTests）
- [x] 键盘焦点测试（KeyboardFocusPanelTests）
- [x] 链接标题服务测试（LinkTitleServiceTests）

---

## 待评估功能项

> 面向后续版本的产品与工程增强建议。

### 1. 隐私与记录控制

- 菜单栏增加监控状态切换项,在当前"打开""设置""退出"之外提供"暂停监控 / 开启监控":
  - 开启状态下显示"暂停监控",点击后停止记录新的剪贴板内容,但不影响已保存历史、收藏、置顶和搜索。
  - 暂停状态下显示"开启监控",点击后恢复剪贴板监听与新内容入库。
  - 菜单栏图标或菜单文案应体现当前监控状态,避免用户忘记已经暂停。
  - 后续可扩展为快速暂停 5 分钟、30 分钟或直到手动恢复。
- 增加敏感内容过滤:疑似密码、验证码、银行卡号、密钥、Token 等内容可自动不记录或弹出提醒。
- 黑名单建议从应用名称升级为 bundle id 匹配,避免同名应用或本地化名称变化导致过滤失效。
- 增加"隐私 App"列表,作为黑名单之外的可记录但默认遮挡机制:
  - 来自隐私 App 的剪贴板内容仍会进入历史记录,但主面板卡片默认隐藏正文、图片缩略图、文件名、链接标题、OCR 文本等敏感信息。
  - 按空格预览、复制、粘贴等会暴露内容的操作前,调用 macOS 系统认证能力验证身份,优先支持 Touch ID、Apple Watch 或本机密码。
  - 认证通过后可在当前面板会话内短暂免重复验证,降低高频查看时的打断感。
  - 第一阶段聚焦 UI 防窥与系统认证,不提供数据层加密;SwiftData 数据库、图片缓存、RTF 数据、OCR 文本等仍可能以明文保存在本机。
  - 后续可评估隐私 App 加密存储:对文本、RTF、OCR、图片缓存等敏感数据做本地加密,密钥存入 Keychain,认证通过后再解密展示。
- 在设置页明确展示收藏/置顶内容是否会被自动清理,增强用户对历史数据保留规则的理解。

### 2. 首次启动与权限引导

- 增加首次启动引导页,解释辅助功能权限的用途,以及未授权时哪些功能不可用。
- 授权后自动检测权限状态并恢复全局快捷键、模拟粘贴等能力。
- 权限异常时在主界面或菜单栏给出轻量提示,而不是让粘贴失败显得像无响应。
- 设置页增加"菜单栏图标常驻"开关,默认开启;关闭前需要明确提示风险,因为菜单栏图标是打开主面板和设置页的重要入口。
  - 如果全局快捷键未注册、辅助功能权限缺失、快捷键冲突或快捷键为空,应禁止关闭菜单栏图标,或在关闭前强制提供其他可用入口。
  - 当用户已关闭菜单栏图标常驻时,再次启动 PasteDeck 应自动打开主面板,并在主面板内保留进入设置和恢复菜单栏图标的入口,避免用户因为快捷键失效而无法进入设置。
  - 当菜单栏图标保持常驻时,启动 PasteDeck 不需要自动打开主面板,继续保持静默后台运行。
  - 当应用已在后台运行且用户再次打开 App 时,应按相同规则处理:菜单栏图标已隐藏则前置主面板;菜单栏图标常驻则不打扰用户。
  - 可评估提供安全重置方式,例如启动时按住 Option 恢复菜单栏图标和默认快捷键。

### 3. 搜索体验增强

- 增加类型筛选:文本、链接、图片、文件、颜色、Markdown、JSON。
- 搜索结果中高亮命中的文本、OCR 内容、来源应用或文件名。
- 支持轻量搜索语法,例如 `type:image`、`app:Safari`、`collection:工作`。
- 保留最近搜索词,方便反复查找常用内容。

### 4. 历史数据管理

- 增加一键清空历史,并支持按内容类型清理。
- 增加历史导出能力,至少支持纯文本/JSON 导出。
- 增加 iCloud 同步能力,让同一 Apple ID 下的多台 Mac 可以同步剪贴板历史与设置:
  - 第一阶段优先同步可完整恢复的结构化内容,例如文本、链接、Markdown、JSON、颜色、RTF 数据、收藏夹、置顶状态、来源应用和创建时间等元数据。
  - 图片内容当前保存为本机缓存路径,不能只同步 `imagePath`;实现前需要把图片缓存升级为可同步资产,或将图片数据作为受控附件同步。
  - 文件类型当前主要保存原始 `filePath`、文件名和大小,不保存文件本体;第一阶段建议只同步文件引用信息,并在其他设备上明确标记"原文件不在本机",避免展示成可直接打开。
  - 不默认同步任意本机文件本体,避免 iCloud 空间占用、隐私泄露和大文件带宽问题;后续可评估用户手动选择"同步文件副本"。
  - 需要补充 iCloud/CloudKit capability、同步开关、同步状态提示、冲突合并、删除同步和 iCloud 不可用时的降级策略。
- 增加缓存与历史占用的更清晰展示,区分数据库、图片缓存、文件引用等来源。

### 5. OCR 后续体验

- 在图片预览窗口展示 OCR 识别文本,并支持一键复制识别结果。
- 为历史图片提供"建立 OCR 索引"手动入口,允许用户主动补全旧图片识别。
- OCR 任务应继续保持后台异步执行,避免阻塞剪贴板监听、面板打开和实时搜索。

### 6. 稳定性与升级

- 为 SwiftData 模型演进补充显式迁移策略,降低用户升级后历史数据不可读的风险。
- 为剪贴板解析、清理策略、搜索过滤、OCR 写回增加单元测试或最小集成测试。
- 全局快捷键录制后检测常见系统快捷键冲突,并给出提示。

### 7. 细节效率

- 状态栏菜单展示最近 5 条内容,支持直接点击复制或粘贴。
- 增强预览窗口的本机资源与外部打开能力:
  - 当复制内容是文本路径时,识别本机存在的文件或文件夹;支持去除首尾空白、展开 `~`、识别 `file://` URL,并避免把普通文本误判为路径。
  - 如果文本路径指向文件,且后缀命中语法高亮或纯文本预览配置,按空格打开预览窗口时提供"预览文件内容"按钮,点击后按现有文件预览规则展示内容;大文件继续遵守预览大小上限与截断提示。
  - 复制的文件、文件夹,以及识别出的文本路径,应支持"在 Finder 中显示"和"用其他 App 打开";文件夹默认打开 Finder,文件默认使用系统默认 App。
  - 图片剪贴板内容和图片文件预览应支持"用其他 App 打开",让用户可以直接交给 Preview、Photoshop、Pixelmator 等外部应用处理。
  - 复制的链接应支持"用浏览器打开",默认使用系统默认浏览器;后续可评估选择指定浏览器打开。
- 收藏夹支持拖拽卡片加入或移动。
